First Time Using Git Set Global Instructions
 git config --global user.name "Beris Dizdar" (Whatever your name is)
	press enter
 git confit --global user.email berisd2010@gmail.com (Your email)
	press enter

To get into a specific folder using Terminal (cd means 'Change Directory)
Use cd ~/Desktop/desired folder Example - cd ~/Desktop/CodePr (CodePr is a folder on the desktop)

git init (Only if it’s firs time working with file)

git status

git add .

git status (optional)

git commit -m ‘Text File’	(The ‘Text File’ is a description of the file you’re working with(It must be added)) (It’s NOT optional))

git remote add origin  ‘then add https:// link from your GitHub account’	(Example ‘git remote add origin git https://github.com/BerisDizdaz/CodePractice.git') (Only have to do this the first time you connect git to GitHub.)

git push -u origin main (only need to specify 'main' the first time) 
=======
