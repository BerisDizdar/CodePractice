cd desktop/‘folder name’	Exmaple- ‘MorePractice’ (This is a folder)

git init (Only if it’s firs time working with file)

git status

git add .

git status (optional)

git commit -m ‘Text File’	(The ‘Text File’ is a description of the file you’re working with (It’s NOT optional))

git remote add origin  ‘then add https:// link from your GitHub account’	(Example ‘git remote add origin git https://github.com/BerisDizdaz/CodePractice.git')

git push -u origin main (only need to specify 'main' the first time) 